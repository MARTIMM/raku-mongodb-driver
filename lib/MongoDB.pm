use v6;

module MongoDB:ver<0.22.1> {
  use MongoDB::Wire;

  use MongoDB::Connection;
  use MongoDB::Database;
  use MongoDB::Collection;
  use MongoDB::Cursor;


#  our $x = 10;
#say "M x: ", $MongoDB::x;
}

